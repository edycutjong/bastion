[package]
edition = "2021"
name = "bastion"
version = "1.1.0"
repository = "https://github.com/edycutjong/bastian"

[dependencies]
odra = { version = "2.7.0", features = [], default-features = false }
odra-casper-livenet-env = { version = "2.7.0", optional = true }

[features]
# Enables the livenet deploy binary: cargo run --bin bastion_livenet --features livenet
livenet = ["odra-casper-livenet-env"]

[dev-dependencies]
odra-test = { version = "2.7.0", features = [], default-features = false }

[build-dependencies]
odra-build = { version = "2.7.0", features = [], default-features = false }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
odra-build = { version = "2.7.0", features = [], default-features = false }

[[bin]]
name = "bastion_build_contract"
path = "bin/build_contract.rs"
test = false

[[bin]]
name = "bastion_build_schema"
path = "bin/build_schema.rs"
test = false

[[bin]]
name = "bastion_livenet"
path = "bin/deploy.rs"
required-features = ["livenet"]
test = false

[profile.release]
codegen-units = 1
lto = true

[profile.dev.package."*"]
opt-level = 3
